root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main.exe ."
  entrypoint = "./tmp/main.exe"
  delay = 500
  exclude_dir = ["tmp", "vendor"]
  include_ext = ["go", "yaml"]
  kill_delay = "0s"
  rerun_delay = 500

[log]
  time = false

[misc]
  clean_on_exit = true
